# YemReact — الموقف يمني. والردّ جاهز.

إعادة بناء كاملة لتجربة YemReact: مكتبة عربية RTL، مظهر ورقي/كهرماني مع وضع ليلي، معاينة فيديو وتنزيل ومشاركة، محفوظات، حسابات فعلية، واستوديو إدارة بصلاحيات.

**نسخة Full-stack تجريبية قابلة للتشغيل، وليست إعلانًا عن اعتماد أمني أو جاهزية Enterprise مطلقة.** المقاطع الحالية نماذج صامتة متحركة من صور مولّدة، وموسومة بذلك داخل الواجهة.

## التشغيل

رفع الفيديو ونشره يحتاجان FFprobe مثبتًا على الخادم (حزمة FFmpeg)، أو تحديد `FFPROBE_PATH`. المدة المسموحة من ثانيتين إلى 60 ثانية، والحجم الأقصى باقٍ 15MB.

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm dev
```

ثم افتح `http://localhost:3000`. الخادم مربوط على `0.0.0.0` للمعاينات الخارجية.

```bash
pnpm build
pnpm start
```

المعاينة المسلّمة تعمل ببناء الإنتاج على المنفذ 3000 لتقليل استهلاك الذاكرة.

**للحسابات داخل منصة المعاينة:** افتح الموقع في تبويب مستقل إذا لم تثبت الجلسة داخل الإطار المضمّن؛ بعض المتصفحات تحجب cookies الطرف الثالث. أبقيت سياسة الجلسة `SameSite=Lax` بدل إضعافها من أجل الإطار.

## حساب المالك

الدخول عبر Google وApple وMicrosoft فقط. تفعيل الخدمات يحتاج إعداد مفاتيح التطبيقات وOAuth وروابط العودة على الخادم؛ راجع [دليل الدخول الاجتماعي](docs/SOCIAL-AUTH.md). لا تظهر أي حقول بريد أو كلمة مرور.

بعد أول دخول اجتماعي، رقِّ الحساب من طرفية الخادم باستخدام معرفه المحلي:

```bash
ADMIN_USER_ID='<existing-social-user-id>' pnpm admin:create
```

الأمر يلغي جلسات الحساب؛ ادخل بالخدمة نفسها مجددًا. للحسابات السابقة، راجع خطوات الربط الآمن في الدليل. لا يوجد ربط تلقائي بمجرد تطابق البريد.

## ما يعمل

- بحث حي يطبّع العربية، تصفية بالفئة والمدة، ترتيب وعرض المزيد.
- معاينة داخل نافذة مع تشغيل حقيقي، تنقل بالأسهم/السحب، تنزيل MP4/WebM ومشاركة الرابط.
- محفوظات محلية للزائر ومجموعة منفصلة في قاعدة البيانات للحساب.
- تسجيل/دخول/خروج، جلسات HttpOnly وصلاحيات عضو/مدير على الخادم.
- إضافة/تعديل/حذف، رفع فيديو وغلاف، قائمة وسائط ومستخدمين، أحداث محلية وسجل عمليات وتصدير.
- مظهر تلقائي/نهاري/ليلي، ثلاثة ألوان مختارة وتخفيف الحركة.
- PWA للصفحات العامة المخزنة مع صفحة Offline؛ لا تُخزّن الحسابات أو الفيديوهات تلقائيًا.
- Metadata، OG، JSON-LD، sitemap، robots، صفحات 404 وحالات خطأ/تحميل.

## الجودة والتوثيق

- [تقرير التسليم](REPORT.md)
- [القياسات وحدود الاختبارات](docs/QUALITY-REPORT.md)
- [المعمارية وقرارات النطاق](docs/ARCHITECTURE.md)
- [مكتبة المكونات، التوكنات والحركات](docs/DESIGN-SYSTEM.md)
- [API](docs/API.md) · [قاعدة البيانات](docs/DATABASE.md)
- [دليل الاستوديو](docs/ADMIN-GUIDE.md)
- [النشر والإعدادات الأمنية](docs/DEPLOYMENT.md)
- [قائمة الوصول](docs/ACCESSIBILITY.md)
- [الخطوات التالية](docs/ROADMAP.md)

```bash
pnpm typecheck
pnpm test
pnpm format:check
```

الاختبارات الموسعة وسجلاتها ولقطات الشاشة في `verification/v4/`، وأدواتها منفصلة في `verification/tools/`.

## قبل النشر العام

اضبط `NEXT_PUBLIC_SITE_URL` إلى نطاق HTTPS فعلي و`DATA_DIR` إلى قرص دائم. أضف نسخًا احتياطية، حدود رفع ومعدل طلبات على مستوى البنية التحتية، تحقق البريد واستعادة الحساب، وفحصًا/تحويلًا للوسائط. استبدل النماذج بمقاطع مرخّصة وأضف ترجمة/نصوصًا لأي صوت.

OAuth مجهّز للمزودين الثلاثة، لكن التفعيل يحتاج مفاتيح حقيقية واختبارًا لدى المزودين. لا يوجد SMTP أو دفع أو تعليقات أو مدونة أو متابعون.
