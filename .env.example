# Set to the final HTTPS public origin before a production build.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Persistent volume, not a stateless/serverless filesystem.
DATA_DIR=./data
# Provision an owner with pnpm admin:create; do not commit actual values.
# ADMIN_EMAIL=owner@example.com
# ADMIN_PASSWORD=replace-with-a-unique-long-password
